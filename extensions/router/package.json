{
  "name": "@partme.ai/openclaw-router",
  "version": "0.1.0",
  "type": "module",
  "description": "企业级消息路由引擎 — 跨渠道消息转发、知识库/记忆自动注入",
  "license": "MIT",
  "main": "index.ts",
  "files": ["index.ts", "src/", "openclaw.plugin.json", "README.md", "LICENSE"],
  "repository": { "type": "git", "url": "https://github.com/partme-ai/openclaw-plugins.git", "directory": "extensions/router" },
  "keywords": ["openclaw", "openclaw-plugin", "router", "message-bus", "enterprise"],
  "scripts": { "build": "tsup", "dev": "tsup --watch", "test": "vitest run", "test:watch": "vitest", "typecheck": "tsc --noEmit", "clean": "rm -rf dist", "prepublishOnly": "pnpm build" },
  "peerDependencies": { "openclaw": ">=2026.4.12" },
  "devDependencies": { "tsup": "^8.2.0", "typescript": "^5.7.0", "vitest": "^4.0.0" }
}
